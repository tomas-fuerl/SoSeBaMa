{
	admin off
	auto_https off
}

:8080 {
	handle /health/gateway {
		header Content-Type application/json
		respond `{"role":"gateway","status":"ready"}` 200
	}

	handle_path /api/* {
		reverse_proxy api:3000
	}

	handle {
		reverse_proxy web:8080
	}
}
