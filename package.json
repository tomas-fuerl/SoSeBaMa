{
  "name": "sobama",
  "version": "0.0.0",
  "private": true,
  "description": "Song-Setlist-Band-Manager monorepo",
  "type": "module",
  "packageManager": "pnpm@11.18.0",
  "engines": {
    "node": "24.18.1",
    "pnpm": "11.18.0"
  },
  "scripts": {
    "browser:smoke": "node tools/check-dev-containers.mjs browser",
    "build": "pnpm --recursive --if-present run build",
    "check": "pnpm format && pnpm lint && pnpm typecheck && pnpm test && pnpm licenses:check && pnpm docs:links",
    "container:build": "docker compose -f compose.yaml -f compose.dev.yaml build",
    "container:check": "pnpm container:config && pnpm container:policy && pnpm container:build",
    "container:cleanup": "node tools/check-dev-containers.mjs cleanup",
    "container:config": "docker compose -f compose.yaml -f compose.dev.yaml config --quiet",
    "container:down": "docker compose -f compose.yaml -f compose.dev.yaml down --remove-orphans --volumes",
    "container:health": "node tools/check-dev-containers.mjs health",
    "container:logs": "node tools/check-dev-containers.mjs logs",
    "container:policy": "vitest run --config vitest.container.config.ts",
    "container:port": "docker compose -f compose.yaml -f compose.dev.yaml port gateway 8080",
    "container:smoke": "node tools/check-dev-containers.mjs smoke",
    "container:status": "docker compose -f compose.yaml -f compose.dev.yaml ps --all",
    "container:up": "docker compose -f compose.yaml -f compose.dev.yaml up --detach --wait",
    "dev:web": "pnpm --filter @sobama/web run dev",
    "docs:links": "node tools/check-markdown-links.mjs",
    "format": "prettier . --check",
    "format:write": "prettier . --write",
    "licenses:check": "node tools/check-licenses.mjs",
    "lint": "eslint . --max-warnings=0",
    "start:api": "pnpm --filter @sobama/api run start",
    "start:worker": "pnpm --filter @sobama/worker run start",
    "test": "pnpm build && pnpm test:architecture && pnpm test:toolchain && pnpm test:tools && pnpm test:processes && pnpm --recursive --if-present run test",
    "test:architecture": "vitest run test/architecture-boundaries.test.ts",
    "test:coverage": "pnpm build && vitest run --config vitest.coverage.config.ts --coverage",
    "test:processes": "vitest run test/runtime-processes.test.ts",
    "test:toolchain": "vitest run test/toolchain-policy.test.ts",
    "test:tools": "vitest run test/check-licenses.test.ts",
    "typecheck": "tsc --project tsconfig.json --noEmit && pnpm --recursive --if-present run typecheck"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "@sobama/eslint-config": "workspace:*",
    "@sobama/typescript-config": "workspace:*",
    "@types/node": "24.13.3",
    "@vitest/coverage-v8": "4.1.10",
    "eslint": "10.8.1",
    "prettier": "3.9.6",
    "typescript": "5.9.3",
    "typescript-eslint": "8.67.0",
    "vitest": "4.1.10"
  }
}
